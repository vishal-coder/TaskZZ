export const updateAgeny = async (req, res) => {
  try {
    // check if ageny exist if not send 404
    //else send response
  } catch (error) {
    console.error(error);
    //  500 Internal Server Error
  }
};
